<!DOCTYPE html>
<html lang="en">
<head>
    <title>Your Stream</title>
    <!-- Bootstrap -->
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom page style -->
    <style type="text/css">
        p {
            margin: 0px
        }
        h5 > a :visited {
            color:#999
        }
    </style>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
    <!-- Latest compiled and minified JavaScript -->
    <script src="js/bootstrap.min.js"></script>
    <!-- Enable tooltips for this page -->
    <script type="text/javascript">
        $(function () {
          $('[data-toggle="tooltip"]').tooltip()
      })
    </script>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class=".col-md-8 .col-md-offset-2">
                <h1>Hello <%= @name %></h1>
                <%=erb :'partials/welcome'%>
            </div>
        </div>
        <div class="row">
            <div class=".col-md-8 .col-md-offset-2">
                <% if !@stream.empty? %>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <tbody>
                            <%@stream.each do |item|
                                url = item[0]
                                title = item[1]
                                source = item[2]
                                author = item[3]
                                %>
                                <tr>
                                    <td>
                                        <h5><a href="<%= @name %>/redirect?url=<%= url %>" target="_blank"><%= title %></a><small> <%= source %>
                                            <% if author.length > 2 && author != "undefined" && author != source %>
                                            by <%= author %>
                                            <%end%></small></h5>
                                    </td>
                                    <td>
                                        <p class="text-right">
                                            <a class="btn btn-success" href="<%= @name %>/like?url=<%= url %>" role="button" data-toggle="tooltip" data-placement="top" title="This item is relevant to your interests and like it"><span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></span></a>
                                            <a class="btn btn-danger" href="<%= @name %>/dislike?url=<%= url %>" role="button" data-toggle="tooltip" data-placement="top" title="This item is relevant to your interests but you don't like it"><span class="glyphicon glyphicon-thumbs-down" aria-hidden="true"></span></a>
                                            <a class="btn btn-primary" href="<%= @name %>/irrelevant?url=<%= url %>" role="button" data-toggle="tooltip" data-placement="top" title="This item is irrelevant to your interests"><span class="glyphicon glyphicon-trash" aria-hidden="true"></a>
                                        </p>
                                    </td>
                                </tr>
                                    <% end %>
                                </tbody>
                            </table>
                        </div>
                        <% end %>
                        <% if @stream.empty? %>
                        <p>Sadly there's nothing new for you to read at the moment.</p>
                        <% end %>
                    </div>
                </div>
            </div>
        </body>